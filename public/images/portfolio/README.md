# Portfolio screenshots

Drop real client-site screenshots here, then uncomment the matching entries in
`portfolioShots` in `lib/assets.ts`. The marquee section on the homepage stays
hidden until that array has at least one entry.

Expected filenames (match the commented entries):

- tax-desktop-hero.png       1440x900 viewport, 2x device scale
- tax-desktop-services.png   1440x900 viewport, 2x device scale
- tax-mobile-hero.png         390x844 viewport, 3x device scale
- tax-mobile-detail.png       390x844 viewport, 3x device scale

Keep desktop and mobile captures at consistent viewports so the cards line up.
